# Cross-Cutting Docs Audit — Fixes

## getting-started.mdx

**Step 5 — Model generation:** Replaced `Migration` class pattern (does not exist in `@roost/orm`) with the correct Drizzle `sqliteTable` column definition inside the model file itself. `makeModel` generates a single file at `src/models/<kebab>.ts`, not a separate migration file in `database/migrations/`.

**Step 6 — Query API:** `Post.query()` does not exist. `QueryBuilder` is started via `Model.where()`. Fixed to `Post.where('id', '>', 0).orderBy('created_at', 'desc').all()`.

**Step 6 — Server function import:** Changed `@tanstack/start` → `@tanstack/react-start` (the correct package name as used in `@roost/start`'s own source).

**Step 6 — Model import path:** Changed `'../models/Post'` → `'../models/post'` to match the kebab-case filename generated by `makeModel`.

---

## tutorials/build-a-chat-app.mdx

**Step 2 — Migration pattern:** Removed `Migration` class with `createTable`/`dropTable` builder (does not exist). Replaced with Drizzle `sqliteTable` schema defined in the model file (`src/models/chat-message.ts`). Updated surrounding prose to reflect that `roost make:model` creates one file, not two.

**Step 2 — Success message:** Removed reference to `Migrated: XXXX_create_chat_messages_table` (wrong — `roost migrate` runs `drizzle-kit push`, not a numbered migration runner). Now says "output from `drizzle-kit push`".

**Step 3 — Agent file title:** `src/agents/ChatAssistant.ts` → `src/agents/chat-assistant.ts` (kebab-case, matching `makeAgent` output).

**Step 3 — Tool import path:** `./tools/SummarizeTool` → `./tools/summarize-tool`.

**Step 4 — Tool file title:** `src/agents/tools/SummarizeTool.ts` → `src/agents/tools/summarize-tool.ts`.

**Step 4 — ChatMessage import:** `'../../models/ChatMessage'` → `'../../models/chat-message'`.

**Step 6 — `createServerFn` import:** `@roost/start` does not export `createServerFn`. Fixed to `@tanstack/react-start`. Also updated `ChatAssistant` and `ChatMessage` import paths to kebab-case.

**Step 8 — Streaming server function:** Same `createServerFn` import fix applied to `src/server/chatStream.ts`.

---

## tutorials/build-a-saas-app.mdx

**Step 4 — Migration pattern:** Replaced `Migration` class with Drizzle `sqliteTable` schema in `src/models/workspace.ts`. Updated prose: `makeModel` creates one file, not a separate migration in `database/migrations/`.

**Step 4 — Success message:** Updated to reference `drizzle-kit push` output. Fixed stale `src/models/Workspace.ts` (PascalCase) reference to `src/models/workspace.ts`.

**Step 5 — `AuthMiddleware` on TanStack route:** `AuthMiddleware` is a Roost `Middleware` and belongs to `Application.useMiddleware()`, not TanStack Start's `createFileRoute` `middleware` array (which doesn't accept Roost middleware instances). Replaced with the correct pattern: register `AuthMiddleware` on the `Application` instance in `src/app.ts`, and removed the middleware import from the route file.

**Step 6 — `SubscribedMiddleware` on TanStack route:** Same issue. Replaced with correct `app.useMiddleware(new SubscribedMiddleware())` pattern in `src/app.ts`. Removed middleware imports from the route file.

**Step 8 — Webhook handler:** Fixed two issues:
  1. Import paths `../../models/Workspace` and `../../jobs/SendWelcomeEmail` changed to kebab-case (`workspace`, `send-welcome-email`).
  2. `Workspace.query().where(...).update(...)` does not exist — `QueryBuilder` has no `.update()` method. Replaced with `Workspace.where(...).first()` + mutate `attributes` + `instance.save()`.

**Step 9 — Job file title:** `src/jobs/SendWelcomeEmail.ts` → `src/jobs/send-welcome-email.ts`. Updated callout reference to match.

---

## tutorials/build-a-task-api.mdx

**Step 2 — Model/migration pattern:** `makeModel` generates one file (`src/models/task.ts`), not a separate `database/migrations/` file. Consolidated the Drizzle `sqliteTable` definition and `Model` class into the single model file. Updated all prose references. Changed success message from `Migrations applied successfully` to `drizzle-kit push` output.

**Step 2 — Model file name:** All references updated from PascalCase `Task.ts`/`models/Task` to kebab-case `task.ts`/`models/task` to match `makeModel` output.

**Step 10 — Relationship migration:** Removed the separate `database/migrations/XXXX_create_tasks_table.ts` snippet; schema change now shown in the model file directly (`src/models/task.ts`). `User` model also moved to `src/models/user.ts` with inline Drizzle schema (no separate migrations file).

**Step 11 — `AuthMiddleware` on `createAPIFileRoute`:** `middleware: [AuthMiddleware]` on TanStack Start's API route is not how Roost middleware works. Replaced with correct pattern: register `AuthMiddleware` via `app.useMiddleware()` in `src/app.ts`. Removed the middleware import from the route file.

**"What you built" — migration wording:** Changed "backed by a D1 SQLite migration" → "backed by a D1 SQLite schema (pushed with `roost migrate`)" to match the actual `drizzle-kit push` mechanism.

---

## tutorials/deploy-to-cloudflare.mdx

**Step 2 — `roost provision`:** This command does not exist in the CLI (`packages/cli/src/index.ts` has no `provision` case). Replaced with the correct Wrangler commands to create a D1 database (`wrangler d1 create`) and KV namespace (`wrangler kv namespace create`) and paste the returned IDs into `wrangler.jsonc`.

**Step 5 — `roost deploy` description:** The claim that `roost deploy` "runs database migrations against your production D1 instance" is false — it runs `vite build` then `wrangler deploy` only. Removed the fake "Running migrations..." output block. Added a callout explaining that migrations must be run separately before or after deploying.

---

## concepts/architecture.mdx, concepts/service-container.mdx, concepts/edge-computing.mdx, concepts/laravel-patterns.mdx, concepts/testing-philosophy.mdx

No changes required. All claims verified against source:
- `RoostContainer` class name, `bind`/`singleton`/`resolve`/`scoped` API — correct.
- Two-phase `register()`/`boot()` lifecycle — correct.
- `Application.handle(request)` signature — correct.
- `roostFn` reference in architecture.mdx — correct (`@roost/start` exports `roostFn`).
- `TestClient`, `setupTestSuite`, `actingAs`, `Agent.fake()`, `Job.fake()` — all verified present with matching signatures.
- Cloudflare binding descriptions (D1, KV, R2, Queues, Workers AI) — accurate.
- Laravel pattern comparisons (no facades, no `__get` magic, explicit container) — accurate.
