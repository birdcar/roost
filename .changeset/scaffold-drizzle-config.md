---
"@roostjs/cli": patch
---

`roost new` now scaffolds `drizzle.config.ts` and a `database/` skeleton (`schema.ts`, `seeds/index.ts`, `migrations/.gitkeep`) so new projects are wired for Drizzle ORM out of the box.
